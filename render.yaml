services:
  - type: web
    name: routeme-v2
    runtime: node
    region: oregon
    rootDir: frontend
    buildCommand: npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20
